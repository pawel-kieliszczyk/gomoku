compiler = ARGUMENTS.get('compiler', 'clang++')


env = Environment(CXX = compiler)
env.Append(CXXFLAGS = ['-std=c++1z', '-Wall', '-Werror'])


SConscript('SConscript-application', variant_dir='bin', exports='env', duplicate=0)
